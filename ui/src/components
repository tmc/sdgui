It seems there's a slight confusion. While you are asking for the file `../ui/src/components`, it's a directory used to store the React components, not a specific file. Here, typically, we would have multiple files, each representing a specific React component. However, I will provide code that demonstrates typical structure and content for two example components - `ProgramInput.tsx` and `FilesOutput.tsx`.

Here is how one might code an example component in TypeScript:

For `ProgramInput.tsx`:
```jsx
import React, { useState } from "react";

const ProgramInput = () => {
  const [program, setProgram] = useState("");

  const handleChange = (event: React.ChangeEvent<HTMLInputElement>) => {
    setProgram(event.target.value);
  }

  const handleSubmit = (event: React.FormEvent) => {
    event.preventDefault();
    // Sent program to backend
  }

  return (
    <form onSubmit={handleSubmit}>
      <label>
        Program Description:
        <input type="text" value={program} onChange={handleChange} />
      </label>
      <input type="submit" value="Submit" />
    </form>
  );
};

export default ProgramInput;
```

For `FilesOutput.tsx`:
```jsx
import React, { useState, useEffect } from "react";

const FilesOutput = () => {
  const [files, setFiles] = useState([]);

  useEffect(() => {
    // Fetch files from backend and update state
  }, []);

  return (
    <div>
      {files.map((file: string, index: number) => (
        <div key={index}>{file}</div>
      ))}
    </div>
  );
};

export default FilesOutput;
```